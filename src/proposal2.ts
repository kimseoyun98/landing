// GlobeAsset moved to Antigravity/globe-asset/ project
// This proposal is archived — re-enable when GlobeAsset is needed

export function initProposal2(_containerElement: HTMLElement) {
  console.warn('Proposal2: GlobeAsset has been moved to globe-asset project');
  return { destroy: () => {} };
}
